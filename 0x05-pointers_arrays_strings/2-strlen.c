#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 *
 * @s: integer
 * Return; l as length of string
 */
int _strlen(char *s)
{
	int n;
	int l = 0;

	for (n = 0; s[n] != '\0'; i++)
	{
		l++;
	}
	return (l);
}
