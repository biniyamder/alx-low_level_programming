#include <stdio.h>
#include "main.h"
/**
 * _islower - a function that checks for lowercase character.
 *
 * Return: 0, 1
 */
int _islower(int c)
{
	if (c = 'a'; c <= 'z'; c++)
	{
		putchar("1");
	}
	else
	{
		putchar("0");
	}
}
